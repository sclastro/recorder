# 專業錄音 App 開發計劃（Android / Samsung S23）

> 目標：做一個比 Samsung 內建錄音機更專業、更靚、更好用嘅錄音 App，
> 支援高質素錄音、多格式儲存、資料夾分類管理、播放同無損剪輯。

---

## 0. 一句話定位

**一個「錄得靚、搵得返、剪得快」嘅專業錄音工具。**

- 錄得靚 → 完整控制取樣率／位元深度／聲道／音源，支援無損 WAV / FLAC
- 搵得返 → 資料夾 + 標籤 + 全文搜尋（連轉文字都可以搜）
- 剪得快 → 無損 trim，唔使重新編碼，秒速出新檔案

---

## 1. 技術架構

### 1.1 基本選型

| 項目 | 選擇 | 原因 |
|---|---|---|
| 語言 | Kotlin 2.x | Android 官方首選 |
| UI | Jetpack Compose + Material 3 Expressive | 做動畫、立體感 UI 最方便 |
| 最低支援 | Android 8.0 (API 26) | 覆蓋率高，你部 S23 遠遠超過 |
| 目標 SDK | API 36 (Android 16) | 上架／安裝都要跟最新規範 |
| 架構 | 單 Activity + MVVM/MVI + Coroutines/Flow | 標準、易測試 |
| DI | Hilt | 減少樣板碼 |
| 資料庫 | Room | 錄音索引、資料夾、標籤、書籤 |
| 設定 | DataStore (Proto) | 取代 SharedPreferences |
| 播放 | Media3 ExoPlayer + MediaSession | 背景播放、通知控制、藍牙／車機 |
| 錄音 | **AudioRecord**（唔用 MediaRecorder） | 見下 |
| 編碼 | MediaCodec + MediaMuxer（+ NDK libFLAC / LAME） | 支援多格式 |

### 1.2 點解要用 AudioRecord 而唔用 MediaRecorder

你要求「可以揀幾多 Hz、幾多 bit」——`MediaRecorder` **根本控制唔到位元深度**，格式亦好有限（基本上淨係 AAC/AMR/3GP）。

用 `AudioRecord` 攞原始 PCM，我哋就可以：

- 自由揀 8k / 16k / 22.05k / 32k / 44.1k / 48k Hz（甚至 96k，睇裝置支援）
- 自由揀 16-bit / 24-bit / 32-bit float PCM
- 單聲道 / 立體聲（S23 有多支咪，可以錄真立體聲）
- 實時攞到波形同音量 dB 值（做視覺化同削波警告）
- 暫停／繼續超簡單（停止寫 frame 就得）
- 一邊錄一邊算好波形峰值檔，之後開檔案唔使再解碼，秒開

**代價**：編碼要自己做（MediaCodec），複雜度高咗，所以第一期先做 WAV + M4A，其他格式分期加。

### 1.3 模組結構

```
:app                     -- 入口、Navigation、主題組裝
:core:designsystem       -- 色票、字體、元件（3D 錄音掣、波形、電平錶）
:core:model              -- 純 Kotlin data class
:core:database           -- Room（Recording / Folder / Tag / Bookmark / Transcript）
:core:datastore          -- 使用者設定
:core:audio              -- 錄音引擎、編碼器、波形分析、剪輯引擎
:core:storage            -- MediaStore / SAF 儲存抽象層
:feature:record          -- 錄音畫面
:feature:library         -- 檔案總覽、資料夾、搜尋
:feature:player          -- 播放器
:feature:editor          -- 剪輯
:feature:settings        -- 設定
```

---

## 2. 功能規格（對應你嘅需求）

### 2.1 錄音（需求 1）

**(a) 撳掣即時錄音**
- 主畫面正中一個大錄音掣，撳一下即刻開始（權限已授權嘅話 0 延遲）
- 三種更快嘅入口：
  - 桌面 Widget（Glance）——一撳即錄，唔使開 App
  - 快速設定磚 Quick Settings Tile——落拉通知欄一撳即錄
  - 長按 App icon 嘅捷徑 shortcut
- 用 **Foreground Service**（`foregroundServiceType="microphone"`）錄，熄螢幕／切走 App 都繼續錄
- ⚠️ Samsung 電池優化好惡，首次啟動要引導你將本 App 設為「不受限制」，唔係後台會被殺

**(b) 錄音質素設定**
- 取樣率：8k / 16k / 22.05k / 32k / 44.1k / 48k（+ 裝置支援嘅 96k）
- 位元深度：16-bit / 24-bit / 32-bit float
- 聲道：單聲道 / 立體聲
- 位元率（有損格式）：64 / 96 / 128 / 160 / 192 / 256 / 320 kbps
- **音源選擇**（呢個好重要，內建 App 冇）：
  - `MIC` — 一般
  - `VOICE_RECOGNITION` — 關咗多數後處理，人聲最乾淨
  - `UNPROCESSED` — 完全冇 AGC / 降噪，錄音樂／樂器必用（S23 支援）
  - `CAMCORDER` — 用主咪，指向性
- 音效開關：回音消除 AEC / 降噪 NS / 自動增益 AGC（逐個獨立開關）
- 輸入裝置選擇：內建咪 / 藍牙咪 / USB-C 外接聲卡或咪
- 一鍵預設 Preset：`語音備忘`（16k mono AAC 64k）、`會議`（44.1k mono AAC 128k）、`訪問`（48k stereo WAV 24-bit）、`音樂`（48k stereo FLAC 24-bit, UNPROCESSED）、`自訂`
- 即時顯示：預估檔案大小／每分鐘 MB、剩餘可錄時間

**(c) 儲存格式**

| 格式 | 副檔名 | 實作方式 | 期數 |
|---|---|---|---|
| PCM WAV（無損） | `.wav` | AudioRecord 直寫 + WAV header | P1 |
| AAC-LC / HE-AAC | `.m4a` | MediaCodec + MediaMuxer(MP4) | P1 |
| Opus（高效率） | `.ogg` | MediaCodec + MediaMuxer(OGG, API 29+) | P2 |
| FLAC（無損壓縮） | `.flac` | NDK libFLAC（MediaMuxer 唔支援 raw FLAC 容器） | P2 |
| MP3（最通用） | `.mp3` | NDK LAME（Android 冇內建 MP3 編碼器） | P3 |

> 亦會提供「錄完之後轉檔」功能，例如錄 WAV 母帶，再匯出一個細粒 M4A 傳俾人。

### 2.2 檔案管理（需求 2）

**(a) 自訂檔案名**
- 檔名範本（token）：`{date}`、`{time}`、`{seq}`、`{preset}`、`{folder}`、`{title}`
  例如 `20260810_1432_會議_客戶A.m4a`
- 錄完即刻彈一個「命名」對話框（可以設定關閉，用範本自動命名）
- 隨時重新命名、支援批次重新命名

**(b) 自訂儲存位置**
- 預設：`Music/Recorder/`（透過 MediaStore，唔使額外權限，其他 App 同電腦都見到）
- 進階：用 SAF（`ACTION_OPEN_DOCUMENT_TREE`）揀任何資料夾，包括 **SD 卡**同 **USB 手指**，並持久化授權
- 每個「資料夾」可以獨立指定實體儲存位置

**(c) 分類系統**
- **實體資料夾**（真係喺檔案系統入面），確保你用電腦／檔案總管都睇到同一個結構——唔會被 App 綁死
- Room 資料庫做索引層，額外提供：
  - 資料夾自訂顏色／圖示
  - **標籤 Tag**（一個檔案可以有多個 tag，跨資料夾）
  - 我的最愛 ⭐、備註文字
  - 搜尋：檔名、備註、標籤、（第三期）轉文字內容全文搜尋
  - 排序／篩選：日期、長度、大小、格式、資料夾、標籤
- **啟動時對帳（reconcile）**：掃描實體目錄，自動補回喺外面新增／刪除／改名嘅檔案，DB 唔會同現實脫節
- **回收桶**：刪除先入回收桶，30 日後自動清（防手殘）

### 2.3 播放與剪輯（需求 3）

**(a) 播放**
- 撳一下就播（列表有迷你波形，播放中會有進度填色）
- 全螢幕播放器：大波形 + 拖曳定位、時間碼精確到 0.01 秒
- 變速 0.5x–3.0x（保持音高）、跳過靜音、A-B 循環、±10 秒快進退
- 背景播放 + 通知／鎖屏／藍牙耳機／車機控制
- 睡眠定時器、音量增益（細聲錄音可以放大）

**(b) 剪輯**
- 波形上兩個把手，拖出想要嘅段落，`只保留選取` / `刪除選取`
- **無損 trim**：
  - WAV → 直接複製 byte 範圍，即時完成，零損失
  - M4A / OGG → 用 MediaExtractor + MediaMuxer 逐個 sample 複製，唔重新編碼（精度到 frame，約 20–40ms，實際用完全夠）
  - 要淡入淡出／轉格式先至重新編碼
- 一律「**另存新檔**」，原檔保留（可選覆寫）
- 其他剪輯：
  - 分割 Split（喺標記位置切開多個檔案）
  - 合併 Merge（幾段錄音接埋一齊）
  - 淡入／淡出、音量正規化 Normalize
  - 自動剪走頭尾靜音
  - 匯入外部音檔嚟剪

---

## 3. 我建議加嘅功能（你冇提但好有用）

### 高價值
1. **錄音中加書籤／標記** — 錄緊會議撳一下標記重點，之後直接跳去嗰個時間點。內建 App 冇，但係最實用嘅功能之一。
2. **實時波形 + dB 電平錶 + 削波警告** — 專業錄音一定要見到訊號有冇爆咗。
3. **崩潰／斷電保護** — 邊錄邊寫，另外寫一個 journal；App 被殺或者冇電，下次開機自動修復未完成嘅錄音，唔會成段冇晒。
4. **語音轉文字（離線）** — 用裝置內建 on-device SpeechRecognizer，或者 NDK 行 whisper.cpp 做完全離線轉錄，帶時間戳，可以搜尋、可以撳字跳去錄音位置。對會議／訪問殺傷力好大。（第三期）
5. **自動分段** — 每 X 分鐘或每 X MB 自動開新檔，長時間錄音唔會得一個超巨型檔。
6. **VOX 聲控錄音** — 低過門檻自動暫停，有聲先錄，慳位。
7. **通話／打斷處理** — 有電話入嚟自動暫停，收線自動繼續；其他 App 搶咪都會通知你。

### 中價值
8. **App 鎖（生物認證）** — 敏感錄音要指紋／人臉先開得。
9. **自動備份** — Google Drive / WebDAV / SMB；或者開個本機 HTTP server，同一個 Wi-Fi 用電腦瀏覽器就下載到，唔使插線。
10. **統計面板** — 總錄音時數、每個資料夾佔幾多空間、儲存清理建議。
11. **匯出／分享** — 分享時可即時轉細碼率，微信／WhatsApp 唔會話太大。
12. **主題** — 深色／淺色／跟系統 + Material You 動態取色 + 幾套專業配色。
13. **繁中／简中／English** 三語。

### 明確唔做（要講清楚）
- ❌ **通話錄音**：Android 10 之後 Google 已經完全封死第三方 App 錄通話，任何聲稱做到嘅都係用無障礙服務嘅灰色手段，會被 Play Store 下架，亦唔穩定。
- ⚠️ **錄其他人要注意法律**：香港《個人資料（私隱）條例》下，錄低同人嘅對話並公開／使用可能構成違規，建議 App 內加一個一次性提示。

---

## 4. 介面設計

### 4.1 設計語言
- **淺色為主**（跟你要求，同 Claude app 嗰種暖白底一致）：
  底色 `#FAF9F5`、卡片 `#FFFFFF`、下沉面 `#F2F0E9`、分隔線 `#E9E6DC`
  文字 `#1F1E1D`／次要 `#6B6963`
- 主色（錄音）：陶土橙 `#C96442`，漸層由 `#E79070` 到 `#97452A`
- 播放色：靜謐綠 `#3E7B6F`；削波警告 `#B3261E`；書籤 `#B8860B`
- 深色主題一樣做齊（底 `#161614`），設定入面可以揀淺色／深色／跟系統
- 字體：時間碼用等寬數字，數字跳動時唔會左右震
- Material 3 Expressive 嘅彈簧動畫（spring）做形狀變化，唔用死板嘅線性動畫

### 4.2 主要畫面

```
┌─ 錄音頁 ─────────────┐  ┌─ 檔案庫 ─────────────┐  ┌─ 播放／剪輯 ────────┐
│  00:03:24.15          │  │ 🔍 搜尋              │  │  大波形 + 選取區      │
│  ▁▃▅█▇▅▃▁▂▄▆█▅▃  ← 實時 │  │ [全部][會議][音樂]…  │  │  ├──────┤            │
│  L ▮▮▮▮▮▯▯ -12dB      │  │ 📁 會議 (23)         │  │  ◀◀ ▶ ▶▶  1.0x       │
│  R ▮▮▮▮▯▯▯ -15dB      │  │ 📁 訪問 (8)          │  │  [裁剪][分割][淡化]  │
│                       │  │ ─────────────────    │  │  [另存新檔]          │
│      ⬤ ← 立體 3D 掣   │  │ ▶ 20260810_客戶A     │  │                      │
│   ⏸  ⏹  🔖           │  │   ▁▃▅▃▁ 12:04  M4A   │  │                      │
│  48kHz·24bit·Stereo   │  │ ▶ 20260809_訪問…     │  │                      │
└───────────────────────┘  └──────────────────────┘  └──────────────────────┘
```

### 4.3 立體感錄音掣（重點）

用 Compose `Canvas` 逐層畫，唔用圖片，任何解析度都清晰。喺淺色底上面，立體感靠光影層次砌出嚟：

1. **外圈光暈**：跟住實時音量脹縮嘅同心圓（錄緊嘅時候好似心跳）
2. **投影**：底下柔和 drop shadow，離地感
3. **球體本體**：45° 徑向漸層（左上光、右下暗）做出球面
4. **內陰影**：邊緣一圈內凹暗邊，做出「按鈕嵌喺面板」嘅感覺
5. **高光**：左上角一片橢圓半透明白光（specular），呢層係「立體」嘅關鍵
6. **邊緣倒角**：1px 亮邊 + 1px 暗邊
7. **按落去**：整體縮 3%、高光位移、陰影收細 + 觸覺回饋（haptic）
8. **狀態變形**：閒置＝圓形；錄緊＝彈簧動畫變成圓角方形（stop 意象），Material 3 shape morph

App icon 同一套視覺：3D 咪高峰，金屬網頭 + 漸層機身 + 高光，做齊 adaptive icon（前景／背景／monochrome 主題化圖示）。

### 4.4 其他 UI 細節
- 列表每個項目有迷你波形縮圖（用錄音時已經算好嘅峰值檔，秒出）
- 底部 mini player，可以上拉展開成全螢幕
- 橫向／平板／摺疊機用雙欄佈局
- 完整 TalkBack 標籤、支援大字體、動態減弱（reduce motion）

---

## 5. 開發分期

| 期 | 內容 | 產出 |
|---|---|---|
| **P0 骨架** | Gradle 專案、模組、主題色票、CI（GitHub Actions 出 APK） | 開得到、裝得到嘅空殼 |
| **P1 MVP** | AudioRecord 引擎 → WAV/M4A、前景服務、實時波形、3D 錄音掣、檔案列表、播放、命名／刪除 | **已經好過內建 App，可以日常用** |
| **P2 核心完成** | 全部質素設定、Opus/FLAC、SAF 自訂位置、資料夾＋標籤＋搜尋、無損剪輯／分割／合併、回收桶 | 你要求嘅功能 100% 齊 |
| **P3 專業化** | 書籤標記、變速播放、跳靜音、Widget、QS 磚、自動分段、VOX、崩潰復原、備份、App 鎖 | 打得贏市面付費 App |
| **P4 加分** | 離線語音轉文字 + 全文搜尋、MP3、統計、多語言 | 差異化功能 |

建議 P1 做完就即刻裝落 S23 用一兩個星期，實際用完先決定 P2 嘅優先次序——真係用先知邊樣最重要。

---

## 6. 點樣裝落你部 S23

你唔一定需要 Android Studio：

1. **GitHub Actions 自動出 APK**（建議）——每次 push 自動 build，你喺手機開 GitHub 網頁直接下載 APK 安裝。呢個 CI 我會喺 P0 一齊整好。
2. 本機 build：Android Studio → USB 連線 → Run。
3. 因為淨係你自己用，唔使上 Play Store，用自簽名 release APK 就得（keystore 要好好保存，換咗 key 就要移除重裝）。

需要嘅權限：`RECORD_AUDIO`、`FOREGROUND_SERVICE`、`FOREGROUND_SERVICE_MICROPHONE`、`POST_NOTIFICATIONS`、（匯入外部音檔時）`READ_MEDIA_AUDIO`。

---

## 7. 風險與對策

| 風險 | 對策 |
|---|---|
| Samsung 後台殺 App，長錄斷咗 | 前景服務 + partial wakelock + 引導關電池優化 + 崩潰復原機制 |
| 部分格式／取樣率裝置唔支援 | 啟動時探測能力，UI 只顯示真正可用嘅選項；失敗自動降級 |
| FLAC / MP3 要 NDK，複雜度高 | 排去 P2/P3，P1 用 WAV + M4A 已經足夠 |
| 無損剪輯精度到 frame 唔到 sample | WAV 做到 sample 精度；壓縮格式提供「精確模式（重新編碼）」選項 |
| 大檔案波形運算慢 | 錄音時同步算峰值，存 `.peaks` 檔；匯入檔案先用背景 worker 算 |
| 儲存權限規則日後再收緊 | 儲存層抽象成 interface（MediaStore / SAF 兩個實作），將來換底層唔使動 UI |

---

## 8. 下一步

等你 review 完呢份 plan，話我知：

1. 有冇邊啲「建議功能」你想要／唔要？
2. 主色想用琥珀橙定另一個顏色？（我可以先做幾個配色 mockup 俾你揀）
3. 確認咗就開 **P0 + P1**，做到一個可以裝落 S23 實際錄到音嘅版本。

---

## 9. 實作進度

| 期 | 狀態 |
|---|---|
| P0 骨架 | ✅ 完成（Gradle 9.7 / AGP 9.3 / Kotlin 2.3 / Compose / Room / Media3，GitHub Actions 自動出 APK） |
| P1 MVP | ✅ 完成（錄音引擎、質素設定、前景服務、3D 錄音掣、檔案庫、播放、剪輯、回收桶、設定） |
| P2 | 部分完成 — 見下 |
| P3 / P4 | 未開始 |

### 已經做咗（超出原本 P1 範圍）
- 完整質素設定：取樣率 8k–96k、16/24/32-bit float、單／立體聲、音源選擇、AEC/NS/AGC
- 三種格式：WAV（無損）、M4A（AAC）、OGG（Opus）
- 資料夾 + 我的最愛 + 搜尋 + 排序
- 無損剪輯（WAV 精確到取樣點，壓縮格式對齊音框）
- 回收桶（可設 7–90 日保留期，自動清理）
- 錄音中書籤、崩潰復原、檔名範本、淺／深主題

### 仲未做（下一步）
- SAF 自訂儲存位置（而家係 App 專屬目錄下嘅真實資料夾樹）
- FLAC（要 NDK libFLAC）、MP3（要 NDK LAME）
- 分割／合併／淡入淡出／正規化
- Widget、Quick Settings 磚、自動分段、VOX
- 離線語音轉文字、雲端備份、App 鎖
